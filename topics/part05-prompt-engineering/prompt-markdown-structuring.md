---
title: プロンプトにおけるMarkdown活用
part: 5
chapter: 第1章 基本原則
tags: [プロンプトエンジニアリング, Markdown, 構造化, 見出し, XMLタグ]
created: 2026-07-05
updated: 2026-07-22
---

# プロンプトにおけるMarkdown活用

## これは何か

長いプロンプトを地の文(段落だけの文章)で書くと、AIは「ここは自分への指示なのか」「ここは参考として渡された資料なのか」を読み違えやすい。見出し(`#`)・箇条書き・番号リスト・コードブロック(` ``` `)・`<context>`のようなXMLタグ(HTMLに似た`<タグ名>〜</タグ名>`で囲む記法)でプロンプトを区切ると、AIが「指示」「背景情報」「参考データ」「出力形式」を取り違えにくくなり、出力の精度・再現性が上がる。[プロンプトの基本構成要素](./prompt-basic-structure.md)が「プロンプトに何を書くか」を扱うページだとすれば、本ページは「それをどう区切って書くか」という書き方の技術に絞って扱う。

## 仕組み・背景

なぜMarkdownやタグでの構造化が効くのか、理由は大きく2つある。

1. **学習データの偏り**: 生成AIの学習データにはプログラムのソースコード、GitHubのREADME、技術ドキュメントなど、Markdown・HTML・XMLが大量に含まれる。そのため`#`は見出し、` ``` `はコード、`-`は箇条書き、`<タグ>`は意味のあるまとまりという「型」をモデルは強く学習しており、同じ記法をプロンプト側で使うと、モデルがその型に沿って情報を正しく仕分けしやすくなる。
2. **文書のパース補助になる**: 「以下が指示です」のような自然文の合図は曖昧になりがち(どこまでが「以下」なのか、次の段落は指示の続きか別の情報かが不明瞭)だが、見出しやタグによる区切りは境界が明確で、指示・文脈・データ・出力形式の取り違えを構造的に防げる。

各社の公式ドキュメントでの推奨は少しずつ異なるが、2026年時点では「区切り記法自体は何でもよいが、1つのプロンプト内では統一する」という結論に収束しつつある。

- **Anthropic(Claude)**: 公式のプロンプトエンジニアリングガイドは、指示・背景・例示・入力データが混在する複雑なプロンプトでは**XMLタグ**(`<instructions>`, `<context>`, `<example>`, `<document>`など)を主たる構造化手段として推奨している。タグ名に決まりはないが、同じプロンプト内では一貫した名前を使うこと、階層がある内容(複数の文書など)は`<documents>`の中に`<document index="1">`のように入れ子にすることが挙げられている。Markdownとタグは競合するものではなく、タグでセクションの「役割」を示し、Markdownでそのセクションの「中身」を整形するという併用が想定されている。この推奨は、Claude Opus 4.8やClaude Sonnet 5など最新モデル世代でも変わっていない。
- **OpenAI(GPT-5系)**: GPT-4.1時代はMarkdown見出し中心の推奨だったが、公式のGPT-5プロンプティングガイドでは**XMLタグによる構造化もあわせて推奨**するようになった。実例として、Cursor社が`<code_editing_rules>`のような`<◯◯_spec>`形式のXMLタグでプロンプトを区切ったところ指示追従性が向上したという知見を紹介している。一方で出力自体のMarkdown装飾については、API経由の既定動作は「アプリ側がMarkdown表示に対応していない可能性を考慮し、指示がない限り最終回答をMarkdownで整形しない」という点に注意が必要(詳細は後述の注意点)。Markdownを使う場合も「コードブロック・箇条書き・表など意味のある箇所に限定する」ことが推奨されている。
- **Google(Gemini、Gemini 3世代)**: 公式ガイドは、XMLタグまたはMarkdown見出しのどちらかで指示とデータの境界を明確にすることを推奨する一方、**同じプロンプト内でXMLとMarkdownを混在させない**ことを勧めている。統一されていない区切り記法は、かえってモデルの境界認識を妨げるためである。またGemini 3は簡潔な指示を好むため、役割設定や制約はプロンプトの冒頭(またはシステムプロンプト相当の欄)に置くことが推奨されている。

長文の資料を読み込ませる場合の並び順にも根拠がある。Anthropicの公式ガイドでは、長文データはプロンプトの先頭に、実際の質問・指示は末尾に置くことで、複雑な複数文書の入力を伴うテストにおいて応答品質が最大30%向上したと報告されている。

## 使いどころ・使い分け

| 状況 | 構造化の要否 |
|---|---|
| 一言の質問・雑談・簡単な言い換え依頼 | 不要。地の文で十分。見出しやタグを付けるとかえって冗長になる |
| 「背景」「資料」「指示」「出力形式」など3ブロック以上を1つのプロンプトに詰め込む | 必須に近い。ブロックごとに見出し・タグで分離する |
| 出力のフォーマットを厳密に固定したい(決まった見出し構成、表、JSON等) | 有効。欲しい出力の型そのものをMarkdownで書いて見せる |
| 長文資料(規約・議事録・レポートなど)を読み込ませて分析・要約させる | 有効。資料をタグで囲み、質問は末尾に置く |
| コード生成・技術文書のレビュー | 必須に近い。コードは必ずコードブロックで囲み、言語名を指定する(` ```python `など) |
| Few-shot(複数の例を見せる)で出力トーンを制御したい | 有効。各例を`<example>`タグ等で囲むと、モデルが「これは例であって指示ではない」と区別しやすい |

判断の目安は「プロンプトの中に性質の異なるブロックが3つ以上あるかどうか」。役割・タスク・出力形式程度のシンプルな依頼なら地の文で書き、資料・複数の指示・出力テンプレートが同時に登場するタスクになったら構造化を検討するとよい。

## 実務での使い方

### Before/After例1: 資料・指示・出力形式が混在するケース

**Before(地の文で全部書いてしまう)**

```
うちは中小企業向けの会計クラウドサービスをやっている会社です。先月の解約率が
急上昇していて、以下が解約理由アンケートの自由記述です。マーケ担当と経営陣に
共有するレポートを作ってください。丁寧な文体で、原因・対策の順に書いてください。
「操作が分かりにくい」「サポートの返信が遅い」「価格が上がった」「他社に乗り換えた」…
(以下、自由記述が続く)
```

このままだと、どこまでが「会社の説明」でどこからが「分析対象のデータ」で、どこからが「指示」なのかがモデルにとって曖昧になりやすい。

**After(見出し・タグで分離)**

```
# 役割
あなたはSaaS企業のカスタマーサクセス責任者です。

<context>
中小企業向け会計クラウドサービスを提供している。先月、解約率が急上昇した。
</context>

<data>
[解約理由アンケートの自由記述をここに貼り付け]
</data>

<instructions>
1. 上記データを解約理由ごとに分類し、件数が多い順に並べてください
2. 各理由について、考えられる根本原因を1〜2行で分析してください
3. 対策案をそれぞれ1つずつ提案してください
</instructions>

<output_format>
- マーケティング担当と経営陣向けの丁寧な文体で書いてください
- 「## 現状」「## 原因別の分析」「## 対策案」の見出し構成にしてください
</output_format>
```

タグ・見出しで区切ることで、「会社説明(context)」「分析対象そのもの(data)」「やってほしいこと(instructions)」「見た目の指定(output_format)」がモデルにとって明確に分離される。

### Before/After例2: 出力フォーマットを厳密に指定したいケース

**Before**: 「比較表にしてまとめて」とだけ指示する → 列の数や順番がAIの気分次第でぶれる。

**After**: 欲しい表の骨組みそのものをMarkdownで書いて見せる。

```
以下のMarkdownテーブルの形式で、3つのプランを比較してください。

| 項目 | プランA | プランB | プランC |
|---|---|---|---|
| 月額料金 | | | |
| 主な機能 | | | |
| 向いている企業規模 | | | |
```

「表にして」という指示より、テンプレートそのものを渡す方が列構成のブレがなくなる。

### コピペで使えるテンプレート: XML+Markdown併用の汎用構造

```
# 役割
[例: "10年の実務経験を持つ社内SEです"]

<context>
[背景・前提条件を2〜3行で]
</context>

<documents>
<document index="1">
<source>[ファイル名や資料の出所]</source>
<document_content>
[本文をそのまま貼り付け]
</document_content>
</document>
</documents>

<instructions>
1. [具体的な手順・依頼を番号で]
2. [...]
</instructions>

<output_format>
[欲しい見出し構成やフォーマットをそのまま書く。例: "## 要約" "## 詳細" "## 次のアクション"]
</output_format>

[ここに実際に聞きたい質問・依頼を最後に置く]
```

長文の資料(`<documents>`)を先頭に、実際の質問を一番最後に置いているのがポイント。この並びだけで応答品質が上がりやすい。

### ツール横断の対応付け(2026年7月時点)

| ツール/モデル系統 | 推奨される区切り記法 | 備考 |
|---|---|---|
| Claude(Anthropic) | XMLタグ(`<context>` `<instructions>` `<example>`など) | 公式ガイドが明示的に推奨。タグ名は自由だが一貫させる。Markdownはタグの中身の整形に使う |
| ChatGPT(GPT-5系) | Markdown見出し(`#`〜`####`)、または`<code_editing_rules>`のようなXMLタグ | 公式のGPT-5プロンプティングガイドは、見出しに加えてXMLタグでの区切りも推奨するようになった。コードは3つのバッククォートで囲む。API既定では最終回答にMarkdownを付けないため、出力にMarkdownを使わせたい場合は明示的に指示する |
| Gemini(Google、Gemini 3系) | XMLタグ**または**Markdown見出し(どちらか一方に統一) | 混在させると境界認識を妨げるため非推奨とされている。役割・制約はプロンプト冒頭に置くと効きやすい |
| Copilot・Dify・社内チャットボット等 | どちらでも動作するが、裏側で使っているモデル(GPT系/Claude系/Gemini系)の流儀に合わせると精度が安定しやすい | プロンプトテンプレートを社内で使い回す場合は、使用モデルを確認してから記法を統一する |

## 注意点・よくある誤解

- **プロンプトの見た目は出力に伝染する**: プロンプトにMarkdown装飾(太字・箇条書き)を多用すると、出力もMarkdownだらけの箇条書きになりやすい。逆に、なめらかな地の文の回答がほしい場合は「Markdownを使うな」と禁止形で書くより、プロンプト自体からMarkdown装飾を減らし、「段落形式のなめらかな文章で書いてください」と肯定形で指示する方が効きやすい。
- **短いタスクへの過剰な構造化は逆効果**: 一言二言の依頼にまでXMLタグや見出しを付けると、かえって冗長になり読みにくくなるだけで精度は上がらない。構造化は「ブロックが複数ある長めのプロンプト」で効果を発揮する技法であり、万能の魔法ではない。
- **記法を途中で混ぜない**: 同じプロンプト内で`<context>`というタグと`# Context`という見出しを両方使うなど、記法を混在させると、モデルが同じ意味の区切りだと認識できないことがある。1つのプロンプトでは記法を統一する。
- **ネストを深くしすぎない**: 見出しをH5・H6まで細分化したり、XMLタグを何重にも入れ子にすると、人間にとっても保守しづらいプロンプトになる。基本は「文書全体を囲むタグ」+「その中の2〜3階層の見出し」で十分なことが多い。
- **見出し名・タグ名は具体的に**: `<context>`や`## 背景`のように内容が想像できる名前を付ける。`<a>`や`## その他`のような曖昧な名前は、区切りとしての効果が薄れる。
- **APIを使う場合は出力のMarkdown有無を明示する**: GPT-5系はAPI経由の既定動作として、指示がない限り最終回答にMarkdown装飾を付けない(呼び出し元アプリがMarkdown表示に対応していない可能性を考慮した仕様)。ChatGPTの通常画面では見え方が異なるため、API連携(Dify・自社ツールなど)で「表や見出し付きで返してほしいのに地の文で返ってくる」場合は、出力形式の指示を明示的に加える。

## 最初の一歩

直近で使った5行以上の長めのプロンプトを1つ選び、「背景」「資料」「指示」「出力形式」の4ブロックに分けて見出しかXMLタグで区切り直し、同じ依頼を投げ直して結果を比較してみる。

## 関連トピック

- [プロンプトの基本構成要素](./prompt-basic-structure.md) — 「プロンプトに何の要素を書くか」を扱う基本ページ。本ページはその要素を「どう区切って書くか」に特化している

## 更新履歴

### 2026-07-22: 各社の最新モデル・公式ガイドの推奨内容を最新化
- **内容**: OpenAIの推奨がGPT-4.1時代の「Markdown見出し中心」から、GPT-5系の公式ガイドでは**XMLタグでの構造化もあわせて推奨**する方向に変化したことを反映(Cursor社の`<code_editing_rules>`等の実例を追記)。あわせて、GPT-5系はAPI既定で最終回答にMarkdownを付けない仕様である点を注意点に追加。Anthropic(Claude Opus 4.8・Claude Sonnet 5など)とGoogle(Gemini 3)の推奨は引き続き有効であることを確認し、モデル世代の表記を更新。ツール横断の対応表・仕組み背景の記述を更新
- **出典**: [Claude Docs: Prompting best practices](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices)、[OpenAI Cookbook: GPT-5 Prompting Guide](https://github.com/openai/openai-cookbook/blob/main/examples/gpt-5/gpt-5_prompting_guide.ipynb)、[Google AI for Developers: Prompt design strategies](https://ai.google.dev/gemini-api/docs/prompting-strategies)、[Philipp Schmid: Gemini 3 Prompting - Best Practices for General Usage](https://www.philschmid.de/gemini-3-prompt-practices)

### 2026-07-05: 初版執筆
- **内容**: プロンプトをMarkdown見出し・箇条書き・コードブロック・XMLタグで構造化する技法について、効く理由(学習データの偏り、文書のパース補助)、Claude/OpenAI/Geminiそれぞれの公式推奨の違い、長文資料を扱う際の並び順(資料を先・指示を後)、Before/Afterのプロンプト例、コピペ用テンプレート、ツール横断の対応表、過剰な構造化への注意点をまとめた
- **出典**: [Claude Docs: Prompting best practices](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices)、[OpenAI Cookbook: GPT-4.1 Prompting Guide](https://developers.openai.com/cookbook/examples/gpt4-1_prompting_guide)、[Google AI for Developers: Prompt design strategies](https://ai.google.dev/gemini-api/docs/prompting-strategies)
