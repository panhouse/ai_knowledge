# ai_knowledge 運用マニュアル

このリポジトリは、毎朝のルーティンで生成AIに関する知識をWeb検索で収集し、
体系化されたトピックページに蓄積していくナレッジベースです。
ルーティンで起動された場合は、必ずこのマニュアルに従って作業してください。

## 収集テーマ

- AIツール・開発手法の新情報、企業・業種別の活用事例、ベストプラクティス
- 業界動向(資金調達・提携・市場・主要プレイヤーの戦略)、規制・ガバナンス
- 上記に限らず、知識体系(Part 1〜11)の各トピックに関連する重要なアップデート

## 知識体系(分類ルール)

収集した情報は `topics/` 配下の Part 1〜11 に分類する。
各パートの章・トピック構成は各ディレクトリの `_index.md` に定義されているので、
分類先を決める前に必ず参照すること。

**どのパートにも素直に当てはまらない情報は、無理に Part 1〜11 へ押し込まず、
Part 12(`topics/part12-uncategorized/`)に一旦集約する。**
Part 12 は一時的な受け皿であり、内容が育って体系側に居場所ができたら移動してよい。

## 1日の更新件数

- 毎朝のルーティンで更新するナレッジは **1日5件** とする
- 候補が5件を超えた場合は、以下の優先度で厳選する
  1. 新規性(既存ページにない新しい情報か)
  2. 影響の大きさ(業界・実務への波及が大きいか)
  3. 知識体系との関連度(Part 1〜11 のトピックを直接更新できるか)
- 確度の高い情報が5件に満たない日は、無理に水増しせず、その日の件数だけでよい

## ナレッジ1件の構成

更新する1件のナレッジは、必ず以下の構成で書く(トピックページの「更新履歴」も
`news.md` への追記も共通)。

```markdown
### YYYY-MM-DD: 見出し(何が起きたかを一言で)
- **内容**: 事実を2〜3行で要約(いつ・誰が・何を)
- **なぜ重要か**: 実務・業界への影響や示唆を1〜2行
- **出典**: [媒体名](URL)
```

- 見出しは後からGrepで見つけやすいよう、固有名詞(サービス名・企業名・規制名など)を含める
- 「なぜ重要か」は感想ではなく、読者(自分)が取るべき行動や判断に繋がる示唆を書く
- 出典が複数ある場合はカンマ区切りで並べてよい

## 毎朝のルーティン手順

1. **収集**: Web検索で前日〜当日の生成AI関連情報を収集する(日本語・英語の両方で検索する)
2. **分類**: 各情報について、知識体系に照らして分類先パートを決め、上記の優先度で **5件に厳選** する(迷ったら Part 12)
3. **重複チェック**: 書き込む前に既存ページをGrepでキーワード検索し、同じ内容を二重に追記しないこと
4. **書き込み**: 以下の判断基準で書く
   - 既存のトピックページがある → 「概要」を必要に応じて最新の内容に書き換え、「更新履歴」の先頭に日付付きで追記
   - 該当ページがなく、今後も続報がありそうなテーマ → `templates/topic-page.md` を雛形に新規ページを作成
   - 単発ニュースで独立ページにするほどではない → そのパート直下の `news.md` に追記(なければ雛形から作成)
5. **目次更新**: 新規ページを作成したら、そのパートの `_index.md` の「収録ページ」一覧に追加する
6. **更新ログ**: `UPDATES.md` の先頭に今朝の更新内容を記録する
   (下記フォーマット。**過去分は削除・編集しない**。全履歴を残す)
7. **コミット**: main に直接コミットして push する(PRは作らない)

### UPDATES.md のフォーマット

```markdown
## YYYY-MM-DD
- [ページタイトル](topics/partXX-xxx/page-name.md): 何を追記・変更したかの一言
- [ページタイトル](topics/partXX-xxx/page-name.md): 新規作成(概要の一言)
```

## 書き方のルール

- ファイル名は英語ケバブケース(例: `eu-ai-act.md`、`function-calling.md`)
- 全トピックページに YAML frontmatter を付ける: `title` / `part` / `chapter` / `tags` / `created` / `updated`
- 本文は日本語で書く。専門用語は原語のままでよい
- **出典URLを必ず残す**。更新履歴の各項目に `([出典](URL))` の形でリンクする
- 「更新履歴」は新しい日付が上
- 「概要」は常に最新の全体像を表すよう上書きで育てる(古くなった記述は書き換える)

## その他

- Part 12 にページが溜まってきたら、既存の体系に移せるもの・新しい章として昇格すべきものがないか
  月1回程度レビューを提案してよい(勝手に体系を変更せず、ユーザーに提案すること)
